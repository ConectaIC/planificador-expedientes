// src/components/ClientCreateExpediente.tsx
'use client';

export default function ClientCreateExpediente() {
  return (
    <button className="icon-btn" title="Nuevo expediente" onClick={() => alert('Crear expediente (pendiente de conectar a modal)')}>
      ➕
    </button>
  );
}
